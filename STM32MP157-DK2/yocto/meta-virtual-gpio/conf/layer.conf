BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "virtual-gpio"
BBFILE_PATTERN_virtual-gpio = "^${LAYERDIR}/"
BBFILE_PRIORITY_virtual-gpio = "8"

LAYERSERIES_COMPAT_virtual-gpio = "scarthgap"
